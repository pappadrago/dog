#ifndef BN_SOUND_ITEMS_H
#define BN_SOUND_ITEMS_H

#include "bn_sound_item.h"

namespace bn::sound_items
{
    constexpr inline sound_item bomb(0);
    constexpr inline sound_item coin(1);
    constexpr inline sound_item die(2);
    constexpr inline sound_item hit(3);
    constexpr inline sound_item jump(4);
    constexpr inline sound_item milestone(5);
    constexpr inline sound_item shot_01(33);
}

#endif

